<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Portfólio — [Seu Nome]</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <style>
    /* ── TOKENS ─────────────────────────────────────────────── */
    :root {
      --ink:        #0f1117;
      --paper:      #f7f6f2;
      --mid:        #6b6b6b;
      --rule:       #d9d6cd;
      --accent:     #1a56db;
      --accent-dim: #d6e4ff;
      --done:       #14532d;
      --done-bg:    #dcfce7;
      --mono:       'JetBrains Mono', monospace;
      --serif:      'DM Serif Display', serif;
      --sans:       'Inter', sans-serif;
    }

    /* ── RESET ──────────────────────────────────────────────── */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      font-family: var(--sans);
      background: var(--paper);
      color: var(--ink);
      font-size: 16px;
      line-height: 1.6;
    }
    a { color: inherit; text-decoration: none; }
    img { max-width: 100%; }

    /* ── NAVIGATION ─────────────────────────────────────────── */
    nav {
      position: sticky;
      top: 0;
      z-index: 100;
      background: rgba(247,246,242,0.92);
      backdrop-filter: blur(8px);
      border-bottom: 1px solid var(--rule);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 2rem;
      height: 56px;
    }
    .nav-logo {
      font-family: var(--mono);
      font-size: 0.85rem;
      color: var(--mid);
      letter-spacing: 0.05em;
    }
    .nav-logo span { color: var(--accent); }
    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }
    .nav-links a {
      font-size: 0.85rem;
      font-weight: 500;
      color: var(--mid);
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--ink); }

    /* ── HERO ───────────────────────────────────────────────── */
    .hero {
      max-width: 860px;
      margin: 0 auto;
      padding: 7rem 2rem 5rem;
    }
    .hero-eyebrow {
      font-family: var(--mono);
      font-size: 0.75rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--mid);
      margin-bottom: 1.5rem;
    }
    .hero h1 {
      font-family: var(--serif);
      font-size: clamp(2.6rem, 6vw, 4.5rem);
      line-height: 1.08;
      font-weight: 400;
      margin-bottom: 1.5rem;
    }
    .hero h1 em {
      font-style: italic;
      color: var(--accent);
    }
    .hero-desc {
      font-size: 1.05rem;
      color: var(--mid);
      max-width: 520px;
      margin-bottom: 2.5rem;
    }
    .hero-meta {
      display: flex;
      gap: 2rem;
      flex-wrap: wrap;
    }
    .hero-meta-item {
      font-family: var(--mono);
      font-size: 0.78rem;
      color: var(--mid);
    }
    .hero-meta-item strong {
      display: block;
      color: var(--ink);
      font-size: 0.92rem;
      font-weight: 500;
      margin-bottom: 0.1rem;
    }

    /* ── DIVIDER ────────────────────────────────────────────── */
    .divider {
      border: none;
      border-top: 1px solid var(--rule);
      margin: 0;
    }

    /* ── SECTION WRAPPER ────────────────────────────────────── */
    .section {
      max-width: 860px;
      margin: 0 auto;
      padding: 4rem 2rem;
    }
    .section-header {
      display: flex;
      align-items: baseline;
      gap: 1rem;
      margin-bottom: 2.5rem;
    }
    .section-label {
      font-family: var(--mono);
      font-size: 0.7rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--mid);
      white-space: nowrap;
    }
    .section-rule {
      flex: 1;
      height: 1px;
      background: var(--rule);
    }
    .section h2 {
      font-family: var(--serif);
      font-size: 2rem;
      font-weight: 400;
      margin-bottom: 0.75rem;
    }

    /* ── ABOUT GRID ─────────────────────────────────────────── */
    .about-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
    }
    .about-block p {
      font-size: 0.95rem;
      color: var(--mid);
      line-height: 1.75;
    }
    .about-block h3 {
      font-family: var(--serif);
      font-size: 1.15rem;
      font-weight: 400;
      margin-bottom: 0.6rem;
    }

    /* ── DELIVERABLES ───────────────────────────────────────── */
    .deliverables-grid {
      display: grid;
      gap: 1px;
      background: var(--rule);
      border: 1px solid var(--rule);
    }
    .card {
      background: var(--paper);
      padding: 1.5rem 1.75rem;
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 1.25rem;
      align-items: start;
      transition: background 0.15s;
    }
    .card:hover { background: #f0efe9; }
    .card-num {
      font-family: var(--mono);
      font-size: 0.7rem;
      color: var(--rule);
      padding-top: 0.25rem;
      min-width: 2rem;
    }
    .card-body { min-width: 0; }
    .card-title {
      font-size: 0.98rem;
      font-weight: 600;
      margin-bottom: 0.3rem;
      color: var(--ink);
    }
    .card-desc {
      font-size: 0.875rem;
      color: var(--mid);
      line-height: 1.6;
      margin-bottom: 0.75rem;
    }
    .tag-row { display: flex; flex-wrap: wrap; gap: 0.4rem; }
    .tag {
      font-family: var(--mono);
      font-size: 0.68rem;
      padding: 0.2rem 0.55rem;
      border-radius: 2px;
      background: var(--accent-dim);
      color: var(--accent);
      letter-spacing: 0.04em;
    }
    .tag.done {
      background: var(--done-bg);
      color: var(--done);
    }
    .card-actions {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      align-items: flex-end;
      min-width: 90px;
    }
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
      font-size: 0.78rem;
      font-weight: 500;
      padding: 0.4rem 0.85rem;
      border-radius: 3px;
      border: 1px solid var(--rule);
      background: white;
      color: var(--ink);
      cursor: pointer;
      transition: border-color 0.15s, background 0.15s;
      white-space: nowrap;
    }
    .btn:hover { border-color: var(--accent); color: var(--accent); background: var(--accent-dim); }
    .btn.primary {
      background: var(--accent);
      color: white;
      border-color: var(--accent);
    }
    .btn.primary:hover { background: #1648c5; border-color: #1648c5; color: white; }

    /* ── STATUS BADGE ───────────────────────────────────────── */
    .status {
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
      font-family: var(--mono);
      font-size: 0.68rem;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }
    .status::before {
      content: '';
      width: 7px;
      height: 7px;
      border-radius: 50%;
      display: inline-block;
    }
    .status.entregue { color: var(--done); }
    .status.entregue::before { background: #22c55e; }
    .status.pendente { color: #92400e; }
    .status.pendente::before { background: #f59e0b; }

    /* ── COMMIT BOX ─────────────────────────────────────────── */
    .commit-box {
      background: var(--ink);
      color: #e2e8f0;
      padding: 1.5rem;
      font-family: var(--mono);
      font-size: 0.82rem;
      line-height: 1.8;
      border-radius: 4px;
    }
    .commit-box .c-label { color: #64748b; }
    .commit-box .c-hash { color: #38bdf8; }
    .commit-box .c-author { color: #a78bfa; }
    .commit-box .c-date { color: #34d399; }
    .commit-box .c-msg { color: #fde68a; }

    /* ── FOOTER ─────────────────────────────────────────────── */
    footer {
      border-top: 1px solid var(--rule);
      padding: 2rem;
      text-align: center;
      font-size: 0.8rem;
      color: var(--mid);
      font-family: var(--mono);
    }

    /* ── RESPONSIVE ─────────────────────────────────────────── */
    @media (max-width: 640px) {
      nav { padding: 0 1rem; }
      .nav-links { gap: 1rem; }
      .hero { padding: 4rem 1rem 3rem; }
      .section { padding: 2.5rem 1rem; }
      .about-grid { grid-template-columns: 1fr; }
      .card { grid-template-columns: auto 1fr; }
      .card-actions { flex-direction: row; flex-wrap: wrap; min-width: unset; }
      .card-num { display: none; }
    }

    /* ── ANIMATIONS ─────────────────────────────────────────── */
    @media (prefers-reduced-motion: no-preference) {
      .card { animation: none; }
      .hero h1, .hero-eyebrow, .hero-desc { opacity: 0; animation: fadeUp 0.5s ease forwards; }
      .hero-eyebrow { animation-delay: 0.05s; }
      .hero h1 { animation-delay: 0.15s; }
      .hero-desc { animation-delay: 0.25s; }
      .hero-meta { opacity: 0; animation: fadeUp 0.5s ease 0.35s forwards; }
      @keyframes fadeUp {
        from { opacity: 0; transform: translateY(14px); }
        to   { opacity: 1; transform: translateY(0); }
      }
    }
  </style>
</head>
<body>

<!-- ── NAV ────────────────────────────────────────────────────── -->
<nav>
  <span class="nav-logo"><span>~/</span>portfolio</span>
  <ul class="nav-links">
    <li><a href="#sobre">Sobre</a></li>
    <li><a href="#entregas">Entregas</a></li>
    <li><a href="#repositorio">Repositório</a></li>
  </ul>
</nav>

<!-- ── HERO ───────────────────────────────────────────────────── -->
<header class="hero">
  <p class="hero-eyebrow">Portfólio Acadêmico · 2025.1</p>
  <h1>
    Um semestre de<br>
    <em>trabalho documentado.</em>
  </h1>
  <p class="hero-desc">
    Coleção das entregas realizadas ao longo do semestre,
    organizadas por atividade — com descrições, artefatos e links de acesso.
  </p>
  <div class="hero-meta">
    <div class="hero-meta-item">
      <strong>[Seu Nome]</strong>
      Nome completo
    </div>
    <div class="hero-meta-item">
      <strong>[Sua Matrícula]</strong>
      Matrícula
    </div>
    <div class="hero-meta-item">
      <strong>[Disciplina]</strong>
      Disciplina
    </div>
    <div class="hero-meta-item">
      <strong>[Professor(a)]</strong>
      Docente
    </div>
  </div>
</header>

<hr class="divider">

<!-- ── SOBRE ──────────────────────────────────────────────────── -->
<section class="section" id="sobre">
  <div class="section-header">
    <span class="section-label">Sobre</span>
    <div class="section-rule"></div>
  </div>
  <h2>Quem sou eu</h2>
  <div class="about-grid">
    <div class="about-block">
      <h3>Apresentação</h3>
      <p>
        Estudante de [Curso] na [Instituição]. Este portfólio reúne as
        atividades desenvolvidas na disciplina de [Disciplina] durante o
        semestre de 2025.1, demonstrando a evolução do aprendizado ao
        longo das semanas.
      </p>
    </div>
    <div class="about-block">
      <h3>Competências trabalhadas</h3>
      <p>
        [Descreva aqui as principais competências que você desenvolveu
        durante o semestre — ex: desenvolvimento web, lógica de
        programação, design de interfaces, etc.]
      </p>
    </div>
  </div>
</section>

<hr class="divider">

<!-- ── ENTREGAS ───────────────────────────────────────────────── -->
<section class="section" id="entregas">
  <div class="section-header">
    <span class="section-label">Entregas</span>
    <div class="section-rule"></div>
  </div>
  <h2>Catálogo de atividades</h2>

  <div class="deliverables-grid">

    <!-- ENTREGA 1 -->
    <article class="card">
      <span class="card-num">01</span>
      <div class="card-body">
        <p class="card-title">Atividade 1 — [Título da Atividade]</p>
        <p class="card-desc">
          [Descreva brevemente o que foi feito nesta atividade, quais
          conceitos foram aplicados e o resultado obtido.]
        </p>
        <div class="tag-row">
          <span class="tag done">✓ Entregue</span>
          <span class="tag">[Tecnologia]</span>
          <span class="tag">[Tema]</span>
        </div>
      </div>
      <div class="card-actions">
        <span class="status entregue">Entregue</span>
        <a href="#" class="btn primary">Ver projeto</a>
        <a href="#" class="btn">Download</a>
      </div>
    </article>

    <!-- ENTREGA 2 -->
    <article class="card">
      <span class="card-num">02</span>
      <div class="card-body">
        <p class="card-title">Atividade 2 — [Título da Atividade]</p>
        <p class="card-desc">
          [Descreva brevemente o que foi feito nesta atividade, quais
          conceitos foram aplicados e o resultado obtido.]
        </p>
        <div class="tag-row">
          <span class="tag done">✓ Entregue</span>
          <span class="tag">[Tecnologia]</span>
          <span class="tag">[Tema]</span>
        </div>
      </div>
      <div class="card-actions">
        <span class="status entregue">Entregue</span>
        <a href="#" class="btn primary">Ver projeto</a>
        <a href="#" class="btn">Download</a>
      </div>
    </article>

    <!-- ENTREGA 3 -->
    <article class="card">
      <span class="card-num">03</span>
      <div class="card-body">
        <p class="card-title">Atividade 3 — [Título da Atividade]</p>
        <p class="card-desc">
          [Descreva brevemente o que foi feito nesta atividade, quais
          conceitos foram aplicados e o resultado obtido.]
        </p>
        <div class="tag-row">
          <span class="tag done">✓ Entregue</span>
          <span class="tag">[Tecnologia]</span>
          <span class="tag">[Tema]</span>
        </div>
      </div>
      <div class="card-actions">
        <span class="status entregue">Entregue</span>
        <a href="#" class="btn primary">Ver projeto</a>
        <a href="#" class="btn">Download</a>
      </div>
    </article>

    <!-- ENTREGA 4 -->
    <article class="card">
      <span class="card-num">04</span>
      <div class="card-body">
        <p class="card-title">Atividade 4 — [Título da Atividade]</p>
        <p class="card-desc">
          [Descreva brevemente o que foi feito nesta atividade, quais
          conceitos foram aplicados e o resultado obtido.]
        </p>
        <div class="tag-row">
          <span class="tag done">✓ Entregue</span>
          <span class="tag">[Tecnologia]</span>
          <span class="tag">[Tema]</span>
        </div>
      </div>
      <div class="card-actions">
        <span class="status entregue">Entregue</span>
        <a href="#" class="btn primary">Ver projeto</a>
        <a href="#" class="btn">Download</a>
      </div>
    </article>

    <!-- ENTREGA 5 — ESTE PORTFÓLIO -->
    <article class="card">
      <span class="card-num">05</span>
      <div class="card-body">
        <p class="card-title">Atividade 5 — Site de Portfólio (esta página)</p>
        <p class="card-desc">
          Desenvolvimento de site de portfólio publicado via GitHub Pages,
          reunindo todas as entregas do semestre com título, descrição e
          links de acesso. Utiliza HTML5 e CSS3 puros, sem frameworks externos.
        </p>
        <div class="tag-row">
          <span class="tag done">✓ Entregue</span>
          <span class="tag">HTML5</span>
          <span class="tag">CSS3</span>
          <span class="tag">GitHub Pages</span>
        </div>
      </div>
      <div class="card-actions">
        <span class="status entregue">Entregue</span>
        <a href="#repositorio" class="btn primary">Ver repo</a>
      </div>
    </article>

    <!-- PLACEHOLDER — adicione mais conforme necessário -->
    <article class="card">
      <span class="card-num">06</span>
      <div class="card-body">
        <p class="card-title">Atividade 6 — [Título da Atividade]</p>
        <p class="card-desc">
          [Descreva brevemente o que foi feito nesta atividade.]
        </p>
        <div class="tag-row">
          <span class="tag">Em andamento</span>
          <span class="tag">[Tecnologia]</span>
        </div>
      </div>
      <div class="card-actions">
        <span class="status pendente">Pendente</span>
        <a href="#" class="btn">Ver projeto</a>
      </div>
    </article>

  </div>
</section>

<hr class="divider">

<!-- ── REPOSITÓRIO ─────────────────────────────────────────────── -->
<section class="section" id="repositorio">
  <div class="section-header">
    <span class="section-label">Repositório</span>
    <div class="section-rule"></div>
  </div>
  <h2>Dados do projeto</h2>

  <div class="commit-box">
    <div><span class="c-label">repositório  </span> <span>github.com/<strong>[seu-usuario]</strong>/<strong>[seu-repo]</strong></span></div>
    <div><span class="c-label">site         </span> <span class="c-hash">[seu-usuario].github.io/[seu-repo]</span></div>
    <div><span class="c-label">commit       </span> <span class="c-hash">[hash-do-commit — ex: a3f7c12]</span></div>
    <div><span class="c-label">autor        </span> <span class="c-author">[Seu Nome] &lt;[seu@email.com]&gt;</span></div>
    <div><span class="c-label">data         </span> <span class="c-date">[data do commit — ex: 2025-06-23]</span></div>
    <div><span class="c-label">mensagem     </span> <span class="c-msg">feat: primeira versão do portfólio publicada</span></div>
  </div>

  <div style="margin-top:1.5rem; display:flex; gap:1rem; flex-wrap:wrap;">
    <a href="https://github.com/[seu-usuario]/[seu-repo]" class="btn primary" target="_blank">
      ↗ Repositório no GitHub
    </a>
    <a href="https://[seu-usuario].github.io/[seu-repo]" class="btn" target="_blank">
      ↗ Site publicado
    </a>
  </div>
</section>

<!-- ── FOOTER ──────────────────────────────────────────────────── -->
<footer>
  [Seu Nome] · [Instituição] · 2025.1 &nbsp;|&nbsp; Publicado via GitHub Pages
</footer>

</body>
</html>
